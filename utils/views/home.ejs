<html>
  <head>
    <meta charset="UTF-8/" />
    <title>Document</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <h4>what is the color of the sky on a clear day?</h4>
    <form action="/result" method="POST">
      <input type="text" name="color" />
      <button>GO</button>
    </form>
    <% if(isRaining) { %>
    <p>It is raining</p>
    <% }else{%>
    <p>it is not raining</p>
    <%} %> <% pets.forEach(pet=>{%>
    <li><%=pet.name%> (<%=pet.species%>)</li>
    <%}) %>
  </body>
</html>
